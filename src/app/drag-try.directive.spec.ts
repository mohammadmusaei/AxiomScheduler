import { DragTryDirective } from './drag-try.directive';

describe('DragTryDirective', () => {
  it('should create an instance', () => {
    const directive = new DragTryDirective();
    expect(directive).toBeTruthy();
  });
});
